[package]
name = "roll"
version = "0.3.12"
authors = ["J/A <archer884@gmail.com>"]
edition = "2018"
description = """
A dice roller.

Use expressions like 2d8+5. Results are printed with the total first, followed
by each individual roll, followed by the fixed modifier (if any). Max rolls
(crits) are highlighted in green, while low rolls are highlighted in red.
"""

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
chrono = "0.4.19"
colored = "2.0.0"
directories = "3.0.2"
either = "1.6.1"
expr = { git = 'https://github.com/archer884/expr.git' }
exprng = { git = 'https://github.com/archer884/exprng.git' }
hashbrown = { version = "0.11.2", features = ["serde"] }
rand = "0.8.3"
regex = "1.5.3"
serde = { version = "1.0.125", features = ["derive"] }
serde_json = "1.0.64"
squirrel-rng = "0.1.1"
structopt = "0.3.21"
thiserror = "1.0.24"
